#!/usr/bin/env bash
# Strip Co-Authored-By: Claude* trailers from commit messages.
# Public repo policy: AI-tool attribution must not appear in commit history.
set -euo pipefail
sed -i.bak -E '/^Co-[Aa]uthored-[Bb]y:[[:space:]]*Claude/d' "$1"
rm -f "$1.bak"
